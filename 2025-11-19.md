


## What are some sqlalchemy parameters for better connection handling?

pool_recycle: recycles connection before i might timeout
pool_pre_ping: tests connection before using it

```python
from sqlalchemy import create_engine

engine = create_engine(
  pool_size=10, # Minimum number of connections to keep open inside the connection pool
  max_overflow=20, # Maximum number of connections to open above pool_size
  pool_timeout=30, # Maximum time to wait for a connection 
  pool_recycle=180, #Recycles connections after this many seconds
  pool_pre_ping=True, # Tests connection before using it

)

```

## What does REST api stand for?
- Representational State Transfer which is an architectural api style. 
- clients interact with resources/data entities
- resources are transferred via representations of their state
- transferring is done using HTTP methods
- representations are made using json, xml, etc.

## What are the main elements of an API?
- data layer: ...
- application layer: ...
- interface layer: ...

